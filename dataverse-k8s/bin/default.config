# Variables used in resource creation

HOST_DNS_ADDRESS=${HOST_DNS_ADDRESS:-localhost}
POSTGRES_SERVER=${POSTGRES_SERVER:-postgres}
POSTGRES_PORT=${POSTGRES_PORT:-5432}
POSTGRES_DATABASE=${POSTGRES_DATABASE:-dvndb}
POSTGRES_USER=${POSTGRES_USER:-dvnapp}
MAIL_SERVER=${MAIL_SERVER:-postfix}
