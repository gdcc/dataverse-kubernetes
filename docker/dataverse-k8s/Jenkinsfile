node {
  def image

  stage('Clone repository') {
    checkout scm
  }

  stage('Build image') {
    image = docker.build("iqss/dataverse-k8s:build-${env.BUILD_ID}", "-f ${dockerfile} ${context}")
  }
}
